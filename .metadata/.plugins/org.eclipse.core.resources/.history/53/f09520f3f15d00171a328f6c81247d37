package ch1;

public class Urlify {

	public static void replace(char [] arr, int trueLength){
		int spaces = countSpaces(arr);
		
	}
	
	public static int countSpaces(char [] arr){
		int count = 0;
		for(int i = 0; i < arr.length; i++) {
			if(arr[i] == " ".charAt(0)){
				count++;
			}
		}
	}
}
